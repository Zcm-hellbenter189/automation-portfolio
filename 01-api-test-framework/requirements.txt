# Requires: Python >= 3.10
# 原因：core/http_client.py 使用了 dict | None 的 PEP 604 联合类型语法（3.10+ 引入）
requests>=2.28
pytest>=7.0
pytest-html>=4.0
PyYAML>=6.0

# CI / 报告增强（可选）：装了才能用 `python run.py --allure` / `--ci`
# 未安装时 run.py 会自动降级跳过 Allure 收集，不影响其他功能
allure-pytest>=2.13